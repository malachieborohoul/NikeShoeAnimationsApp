import 'package:flutter/material.dart';

class NikeShoesDetails extends StatelessWidget {
  const NikeShoesDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}